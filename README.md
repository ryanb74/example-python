# example-python

A basic Python project scaffolded with `uv`.

## Installation

```bash
uv sync
```

## Usage

Run the main script:

```bash
uv run example-python
```

## Testing

Run tests with `pytest`:

```bash
uv run pytest
```
